# Dev proxy target when the scheduler is not on 8080 (matches SERVER_PORT on the API).
# VITE_API_PROXY_TARGET=http://localhost:8081

# Optional: call API directly from the browser (requires CORS). Leave unset to use Vite dev proxy.
# VITE_API_BASE_URL=http://localhost:8080
